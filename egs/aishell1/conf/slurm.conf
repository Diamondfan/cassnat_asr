command sbatch --export=PATH --ntasks-per-node=1 --partition=v100 
option mem=* --mem-per-cpu=$0
option mem=0          # This is default option of mem? Do not add anything to qsub_opts
option num_threads=* --cpus-per-task=$0 --ntasks-per-node=1
option num_threads=1 --cpus-per-task=1  --ntasks-per-node=1 # This is default option of num_threads ... ? Do not add anything to qsub_opts
option time=* --time $0     # No default 
option max_jobs_run=5000    # Do nothing
# default gpu=0
# option gpu=0 -p shared
# option gpu=* -p gpu --gres=gpu:$0 --time 4:0:0  # this has to be figured out
# option gpu=* -N1 -n1 -p gpu --mem=4GB --gres=gpu:$0 --cpus-per-task=6 --time=72:0:0  # in reality, we probably should have --cpus-per-task=$((6*$0))
option gpu=* -N1 -n1 --partition=v100 --mem=4GB --gres=gpu:$0 --cpus-per-task=6 --time=72:0:0  # in reality, we probably should have --cpus-per-task=$((6*$0))
option gpu=0

